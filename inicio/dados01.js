// variável (let ou const) armazena um valor (numérico, string, booleano, objeto ou array) que pode ser chamado pelo nome dessa variável.
let nome = "Lapiseira"; // String(Texto).
let quantidade = 10; // Numérico.
let preco = 6.4;
let imposto = 1.5;
let precoFinal = preco + imposto;
let fragil = false; // Booleano.

console.log(nome);
console.log(quantidade);
console.log(preco);
console.log(imposto);
console.log(precoFinal);
console.log(fragil);

nome = "Lapiseira BIC";
console.log(nome);